# Qookie Environment Configuration
# Copy this file to .env and replace the placeholder values with your actual API keys

# Required: Anthropic Claude API key for AI-powered case study generation
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional: GitHub Personal Access Token for case study backup feature
# Create a token at: https://github.com/settings/tokens
# Required scopes: repo (for private repos) or public_repo (for public repos)
GITHUB_TOKEN=your_github_personal_access_token_here

# Required for GitHub backup: Your GitHub username
GITHUB_REPO_OWNER=your_github_username_here

# Required for GitHub backup: Repository name where case studies will be backed up
# This repository should exist and be accessible with your GitHub token
GITHUB_REPO_NAME=your_backup_repository_name_here

# Optional: Serper API key for enhanced web search capabilities
# Get your API key from: https://serper.dev/
SERPER_API_KEY=your_serper_api_key_here

# Setup Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your actual API keys and configuration
# 3. Never commit .env to git - it's already in .gitignore
# 4. The app will work with just ANTHROPIC_API_KEY for basic functionality